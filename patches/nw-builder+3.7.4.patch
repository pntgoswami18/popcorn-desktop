--- a/node_modules/nw-builder/lib/Version.cjs
+++ b/node_modules/nw-builder/lib/Version.cjs
@@ -39,6 +39,8 @@
     };
   } else {
     var fixPlatformName = function (platform) {
+      // ARM64 platform names (e.g. 'osx-arm64') already have the correct format.
+      if (platform.indexOf('arm') !== -1) return platform;
       return platform.replace("32", "-ia32").replace("64", "-x64");
     };
 

--- a/node_modules/nw-builder/lib/index.cjs
+++ b/node_modules/nw-builder/lib/index.cjs
@@ -667,7 +667,7 @@
       self._files.forEach(function (file) {
         var dest;
 
-        if (name == "osx32" || name === "osx64") {
+        if (name == "osx32" || name === "osx64" || name === "osx-arm64") {
           dest = path.resolve(
             self.getResourcesDirectoryPath(platform),
             "app.nw",
@@ -692,7 +692,7 @@
           self,
         ),
       );
-    } else if (name == "osx32" || name == "osx64") {
+    } else if (name == "osx32" || name == "osx64" || name === "osx-arm64") {
       // zip just copy the app.nw
       copyPromises.push(
         Utils.copyFile(
@@ -745,7 +745,7 @@
     allDone = [];
 
   this._forEachPlatform(function (name, platform) {
-    if (["osx32", "osx64"].indexOf(name) < 0) return;
+    if (["osx32", "osx64", "osx-arm64"].indexOf(name) < 0) return;
 
     // Let's first handle the mac icon
     if (self.options.macIcns) {
